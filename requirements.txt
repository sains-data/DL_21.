streamlit==1.28.2
# TensorFlow removed so app can run with demo fallback when TF is unavailable in the runtime.
# To use the real model, install a compatible tensorflow build locally or add it back when
# the deployment environment supports matching protobuf/numpy versions.
numpy==1.26.2
pandas==2.0.3
plotly==6.3.1
# Pin Pillow to a version with prebuilt wheels to avoid build-from-source errors on the
# deployment builder (reverts to a broadly-compatible binary wheel release).
pillow==9.5.0
