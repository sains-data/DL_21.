streamlit==1.28.2
# TensorFlow removed so app can run with demo fallback when TF is unavailable in the runtime.
# To use the real model, install a compatible tensorflow build locally or add it back when
# the deployment environment supports matching protobuf/numpy versions.
numpy==1.26.2
pandas==2.0.3
plotly==6.3.1
# Pillow removed to avoid build-from-source failures on the deployment builder.
# The app handles missing Pillow at runtime; to enable image features install Pillow
# locally or add a compatible pillow wheel for the deployment environment.
